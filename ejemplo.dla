#define public__ name = 'ejemplodlib'
#define private__ libpart = 'librarypart-1'
#define private__ multiuse = 'True'
#define public__ propietary_program = 'Shared File System'

// Se define el lenguaje de programacion que contiene la libreria
#define private__ language = 'Python'

block: 'clase_prueba' ; {
	@[referential : "SI"] (
		BEGIN
		print("Sirve")
		END
	)
}
